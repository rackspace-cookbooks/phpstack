server {
  listen  127.0.0.1:<%= node['nginx']['status']['port'] %>;
  server_name _;
  location /server-status {
        stub_status on;
        allow 127.0.0.1;
        deny all;
        access_log off;
  }
  stub_status on;
  access_log off;
}
